import React from 'react';
import './Hero.css';

const Hero: React.FC = () => {
  return (
    <section className="hero">
      {/* Hero section content will go here */}
    </section>
  );
};

export default Hero; 